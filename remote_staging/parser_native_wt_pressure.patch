diff --git a/mpn_melting/parser.py b/mpn_melting/parser.py
index 5195ef2..9dc5f12 100644
--- a/mpn_melting/parser.py
+++ b/mpn_melting/parser.py
@@ -19,7 +19,7 @@ class ParsedSeries:
 NUMBER = r"[-+]?(?:\d+(?:\.\d*)?|\.\d+)(?:[eE][-+]?\d+)?"
 RY_TO_EV = 13.605693122994
 MD_OBSERVABLE_ROW = re.compile(
-    rf"^\s*({NUMBER})\s+({NUMBER})\s+({NUMBER})\s+({NUMBER})\s*$"
+    rf"^\s*({NUMBER})\s+({NUMBER})\s+({NUMBER})\s+({NUMBER})(?:\s+({NUMBER}))?\s*$"
 )
 
 ENERGY_PATTERNS = [
@@ -59,6 +59,8 @@ def parse_text(text: str) -> ParsedSeries:
                 md_potential_energies.append(md_potential)
                 md_kinetic_energies.append(md_kinetic)
                 temperatures.append(float(md_match.group(4)))
+                if md_match.group(5) is not None:
+                    pressures.append(float(md_match.group(5)))
                 expect_md_observable_row = False
                 continue
             if line.strip() and not set(line.strip()) <= {"-"}:
diff --git a/tests/test_parser.py b/tests/test_parser.py
index 71ed3af..a7e9bc7 100644
--- a/tests/test_parser.py
+++ b/tests/test_parser.py
@@ -38,5 +38,19 @@ class ParserTests(unittest.TestCase):
         self.assertAlmostEqual(parsed.energies[-1], -3.61112431 * RY_TO_EV, places=9)
 
+    def test_parse_native_md_observable_row_with_pressure(self):
+        parsed = parse_text(
+            " Energy (Ry)         Potential (Ry)      Kinetic (Ry)        "
+            "Temperature (K)     Pressure (kbar)\n"
+            " ------------------------------------------------------------------------------------------------\n"
+            " -451.03897          -452.03518          0.9962168           "
+            "980                 2.3284297\n"
+        )
+        self.assertEqual(parsed.temperatures, [980.0])
+        self.assertEqual(parsed.pressures, [2.3284297])
+        self.assertEqual(parsed.md_energies, [-451.03897 * RY_TO_EV])
+        self.assertEqual(parsed.md_potential_energies, [-452.03518 * RY_TO_EV])
+        self.assertEqual(parsed.md_kinetic_energies, [0.9962168 * RY_TO_EV])
+
     def test_ignore_timing_temp_label(self):
         parsed = parse_text(
             "                 DiagSub::temp          4.822\n"
