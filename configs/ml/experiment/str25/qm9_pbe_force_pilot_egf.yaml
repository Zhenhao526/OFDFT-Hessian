# @package _global_

defaults:
  - str25/default
  - override /data: qm9_pbe_force_pilot
  - override /model/loss_function: l1_force

tags: ["qm9_pbe_force_pilot", "egf", "kin_plus_xc", "graphformer", "force"]

model:
  force_supervision: true

trainer:
  max_epochs: 20

data:
  datamodule:
    batch_size: 16
    num_workers: 4
    dataset_kwargs:
      load_force_label: true
